namespace: netbird

resources:
  - ../../base/netbird-dashboard/
  - secrets.yaml
  - httproute.yaml

patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: netbird-dashboard
    path: dashboard-env-patch.yaml
