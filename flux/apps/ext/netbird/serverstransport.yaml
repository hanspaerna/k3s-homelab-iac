apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: netbird-signal-transport
  namespace: netbird
spec:
  forwardingTimeouts:
    responseHeaderTimeout: 0s  # No timeout for long-lived gRPC streams
    idleConnTimeout: 90s
    dialTimeout: 30s
