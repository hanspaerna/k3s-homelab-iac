# Generated with Renderer.sh at 2026-01-05T23:46:26+0100
# Chart version: 9.1.0
# Repo name: coturn
# Repo: https://small-hack.github.io/coturn-chart/
# Chart name: coturn
---
# Source: coturn/templates/auth-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: netbird-coturn-auth-secret
  labels:

    helm.sh/chart: coturn-9.1.0
    app.kubernetes.io/version: "4.7.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: "coturn"
data:
  username: "Y290dXJu"
  password: "QU81UFY1MmpoQ2dLbG9WbllLbjl2U0dP"
---
# Source: coturn/templates/configmap-extra.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: netbird-coturn-extra-config
  labels:

    helm.sh/chart: coturn-9.1.0
    app.kubernetes.io/version: "4.7.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: "coturn"
data:
  turnserver.conf:     |
      verbose
---
# Source: coturn/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: netbird-coturn-initial-config
  labels:

    helm.sh/chart: coturn-9.1.0
    app.kubernetes.io/version: "4.7.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: "coturn"
data:
  turnserver.conf: |-
    realm: turn.example.com
    listening-ip: 0.0.0.0
    listening-port: 3478
    tls-listening-port: 5349
    min-port: 49152
    max-port: 65535
    log-file: stdout
    pidfile: "/var/tmp/turnserver.pid"
    pkey: "/tls/tls.key"
    cert: "/tls/tls.crt"
    userdb: "/var/db/turndb"
---
# Source: coturn/templates/service-tcp.yaml
apiVersion: v1
kind: Service
metadata:
  name: netbird-coturn-turn-tcp
  annotations:
spec:
  ports:
    - name: turn-tcp
      port: 3478
      protocol: TCP
      targetPort: 3478
    - name: turn-tcp-tls
      port: 5349
      protocol: TCP
      targetPort: 5349
  selector:
    app.kubernetes.io/name: netbird-coturn-coturn
    app.kubernetes.io/instance: netbird-coturn
  type: ClusterIP
---
# Source: coturn/templates/service-udp.yaml
apiVersion: v1
kind: Service
metadata:
  name: netbird-coturn-turn-udp
  annotations:
spec:
  ports:
    - name: turn-udp
      port: 3478
      protocol: UDP
      targetPort: 3478
    - name: turn-udp-dtls
      port: 5349
      protocol: UDP
      targetPort: 5349
  selector:
    app.kubernetes.io/name: netbird-coturn-coturn
    app.kubernetes.io/instance: netbird-coturn
  type: ClusterIP
---
# Source: coturn/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: netbird-coturn-coturn
  labels:
    helm.sh/chart: coturn-9.1.0
    app.kubernetes.io/version: "4.7.0"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: "coturn"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: netbird-coturn-coturn
      app.kubernetes.io/instance: netbird-coturn
  template:
    metadata:
      labels:
        app.kubernetes.io/name: netbird-coturn-coturn
        app.kubernetes.io/instance: netbird-coturn
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: add-secret-values-to-config
          image: mikefarah/yq:latest
          imagePullPolicy: Always
          env:
            - name: USER
              valueFrom:
                secretKeyRef:
                  name: netbird-coturn-auth-secret
                  key: username
            - name: PASSWORD
              valueFrom:
                secretKeyRef:
                  name: netbird-coturn-auth-secret
                  key: password
          command:
            - /bin/sh
            - -ec
            - |
              cp /initial/turnserver.conf /data/turnserver.yaml && \
              export USER_STR="$USER:$PASSWORD" && \
              yq eval -i '.user = env(USER_STR)' /data/turnserver.yaml && \
              sed -i 's/: /=/' /data/turnserver.yaml && \
              cat /extra/turnserver.conf >> /data/turnserver.yaml && \
              echo '' >> /data/turnserver.yaml && \
              echo 'lt-cred-mech' >> /data/turnserver.yaml && \
              mv /data/turnserver.yaml /data/turnserver.conf
          volumeMounts:
            - name: netbird-coturn-initial-config
              mountPath: "/initial"
            - name: netbird-coturn-extra-config
              mountPath: /extra
            - name: netbird-coturn-config
              mountPath: /data
          securityContext:
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
      containers:
        - name: "coturn"
          image: coturn/coturn:4.7.0
          imagePullPolicy: IfNotPresent
          args: ["-c", "/turnserver.conf"]
          ports:
            - name: turn-udp
              containerPort: 3478
              protocol: UDP
            - name: turn-tcp
              containerPort: 3478
              protocol: TCP
            - name: turn-tcp-tls
              containerPort: 5349
              protocol: TCP
            - name: turn-udp-dtls
              containerPort: 5349
              protocol: UDP
          volumeMounts:
            - name: netbird-coturn-config
              mountPath: "/turnserver.conf"
              subPath: turnserver.conf
              readOnly: true
            - name: var-tmp
              mountPath: /var/tmp
            - name: sqllite
              mountPath: /var/db
          securityContext:
            capabilities:
              add:
                - NET_BIND_SERVICE
              drop:
                - ALL
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
      volumes:
        - name: netbird-coturn-initial-config
          configMap:
            name: netbird-coturn-initial-config
        - name: netbird-coturn-extra-config
          configMap:
            name: netbird-coturn-extra-config
        - name: netbird-coturn-config
          emptyDir: {}
        - name: var-tmp
          emptyDir: {}
        - name: sqllite
          emptyDir: {}
