apiVersion: v1
kind: Service
metadata:
  name: transmission-svc
  labels:
    app.kubernetes.io/name: transmission
    app.kubernetes.io/instance: transmission
spec:
  selector:
    app.kubernetes.io/name: transmission
    app.kubernetes.io/instance: transmission
  ports:
    - name: http
      protocol: TCP
      port: 9091
      targetPort: 9091
  type: ClusterIP
