apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: koito-route
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik-int
  hostnames:
    - "koito.vpn.fingol.pro"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: koito-svc
          port: 4110
