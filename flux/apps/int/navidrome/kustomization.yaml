namespace: navidrome

resources:
  - manifest.yaml
  - httproute.yaml
  - storage.yaml
  - ../../../shared-resources/restic-secret/
  - backup.yaml

patches:
  - target:
      kind: Secret
      name: restic-config
    patch: |
      - op: replace
        path: /stringData/RESTIC_REPOSITORY
        value: rest:http://truenas.lan:8888/navidrome
