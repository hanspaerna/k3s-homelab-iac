apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radicale-route
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik-int
  hostnames:
    - "radicale.vpn.fingol.pro"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: radicale-svc
          port: 5232
