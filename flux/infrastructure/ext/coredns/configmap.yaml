apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
  int.cluster.local.server: |
    # Forward queries for internal cluster to its CoreDNS
    int.cluster.local:53 {
        errors
        cache 30
        forward . 10.43.0.10
    }
